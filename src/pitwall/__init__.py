"""PITWALL: the two desktop windows that replace the Qt dashboard.

React built to static files, hosted in an OS webview, driven by the arcade's
existing TCP broadcast. The package is importable without a system webview:
only `__main__` touches pywebview.
"""
